SELECT COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base;
SELECT COUNT(Number) FROM base WHERE Cost IS NOT NULL;
SELECT New, COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY New;
SELECT YEAR(ReleaseDate), COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY YEAR(ReleaseDate);
SELECT YEAR(ReleaseDate), COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base WHERE Cost BETWEEN 10 AND 20 GROUP BY YEAR(ReleaseDate);
SELECT YEAR(ReleaseDate), COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY YEAR(ReleaseDate) ORDER BY COUNT(Number) DESC;
SELECT COUNT(BookCode), COUNT(DISTINCT BookCode) FROM base;
SELECT LEFT(Title,1), COUNT(BookCode), SUM(Cost) FROM base GROUP BY LEFT(Title,1);
SELECT LEFT(Title,1), COUNT(BookCode), SUM(Cost) FROM base WHERE LEFT(Title,1) BETWEEN 'А' AND 'Я' GROUP BY LEFT(Title,1);
SELECT LEFT(Title,1), COUNT(BookCode), SUM(Cost) FROM base WHERE YEAR(ReleaseDate)>2000 AND LEFT(Title,1) BETWEEN 'А' AND 'Я' GROUP BY LEFT(Title,1);
SELECT LEFT(Title,1), COUNT(BookCode), SUM(Cost) FROM base WHERE YEAR(ReleaseDate)>2000 AND LEFT(Title,1) BETWEEN 'А' AND 'Я' GROUP BY LEFT(Title,1) DESC;
SELECT YEAR(ReleaseDate), MONTH(ReleaseDate), COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY YEAR(ReleaseDate), MONTH(ReleaseDate);
SELECT YEAR(ReleaseDate), MONTH(ReleaseDate), COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base WHERE ReleaseDate IS NOT NULL GROUP BY YEAR(ReleaseDate), MONTH(ReleaseDate);
SELECT YEAR(ReleaseDate), MONTH(ReleaseDate), COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY YEAR(ReleaseDate) DESC, MONTH(ReleaseDate) ASC;
SELECT New AS 'Новая ли', SUM(Cost) AS 'Общая цена в долларах', SUM(Cost*27.95) AS 'Общая цена в грн', SUM(Cost*0.83) AS 'Общая цена в евро', SUM(Cost*74.69) AS 'Общая цена в руб' FROM base GROUP BY New;
SELECT New AS 'Новая ли', ROUND(SUM(Cost)) AS 'Общая цена в долларах', ROUND(SUM(Cost*27.95)) AS 'Общая цена в грн', ROUND(SUM(Cost*0.83)) AS 'Общая цена в евро', ROUND(SUM(Cost*74.69)) AS 'Общая цена в руб' FROM base GROUP BY New;
SELECT Producer, COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY Producer;
SELECT Producer, Theme, COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY Producer, Theme ORDER BY Producer;
SELECT Category, Theme, Producer, COUNT(Number), SUM(Cost), AVG(Cost), MIN(Cost), MAX(Cost) FROM base GROUP BY Category, Theme, Producer ORDER BY Producer, Theme, Category;
SELECT Producer, SUM(ROUND(Cost*27.95)), SUM(Pages) FROM base WHERE (Cost*27.95)/Pages>0.10 GROUP BY Producer;